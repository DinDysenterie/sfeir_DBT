# packages.yml
# Place this file at the same level as your dbt_project.yml

packages:
  # 1. Install a package from dbt Hub (version pinned for reproducibility)
  - package: dbt-labs/dbt_utils
    version: 1.1.1
  - package: calogica/dbt_expectations
    version: 0.10.1